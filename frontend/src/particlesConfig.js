const particlesConfig = {
    particles: {
        number: { value: 50 },
        size: { value: 3 },
        move: { speed: 2 },
    },
    interactivity: {
        events: { onHover: { enable: true, mode: "repulse" } },
    },
};

export default particlesConfig;
